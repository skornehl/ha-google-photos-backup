{
  "config": {
    "step": {
      "user": {
        "title": "Set up Google Photos Backup",
        "description": "Choose which backend should be used to back up photos/videos. See the README for details - in short: 'takeout' is currently the only way to fully back up an existing library.",
        "data": {
          "backend": "Backend"
        }
      },
      "pick_implementation": {
        "title": "Pick Google account"
      },
      "library_api_options": {
        "title": "Target directory & interval",
        "description": "Note: this backend can only see media uploaded by this integration itself, plus whatever you manually select via a Picker session (start_picker_session service). It does NOT automatically read your whole existing library.",
        "data": {
          "target_dir": "Target directory (local mount)",
          "sync_interval_minutes": "Sync interval (minutes)",
          "bandwidth_limit_kbps": "Bandwidth limit (KiB/s, 0 = unlimited)"
        }
      },
      "rclone": {
        "title": "Configure rclone backend",
        "description": "Note: since March 2025, rclone's googlephotos backend can also only download files it uploaded itself. For an existing library the Takeout backend is usually the better choice.",
        "data": {
          "target_dir": "Target directory (local mount)",
          "sync_interval_minutes": "Sync interval (minutes)",
          "rclone_remote_name": "rclone remote name (from rclone.conf)",
          "rclone_binary": "rclone binary path/name",
          "rclone_config_path": "Path to rclone.conf (empty = default)",
          "rclone_source_path": "Source path in remote (e.g. media/by-month)",
          "bandwidth_limit_kbps": "Bandwidth limit (KiB/s, 0 = unlimited)"
        }
      },
      "takeout": {
        "title": "Configure Takeout backend",
        "description": "Drop Google Takeout export archives (.zip/.tgz) into the watch directory - the integration extracts them, files media by capture date from the JSON sidecar, and deduplicates via file hashes.",
        "data": {
          "target_dir": "Target directory (local mount)",
          "sync_interval_minutes": "Sync interval (minutes)",
          "takeout_watch_dir": "Watch directory for Takeout archives",
          "takeout_delete_after_import": "Delete archive after successful import"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This instance is already configured",
      "oauth_error": "Error receiving OAuth2 token data",
      "oauth_timeout": "Timed out waiting for authorization",
      "missing_configuration": "The integration is not configured correctly (Application Credentials missing). See README."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Google Photos Backup options",
        "data": {
          "sync_interval_minutes": "Sync interval (minutes)",
          "bandwidth_limit_kbps": "Bandwidth limit (KiB/s, 0 = unlimited)"
        }
      }
    }
  },
  "selector": {
    "backend": {
      "options": {
        "library_api": "Library/Picker API (manual selection only, no full backup)",
        "rclone": "rclone googlephotos backend",
        "takeout": "Google Takeout archive import (recommended for full backup)"
      }
    }
  },
  "entity": {
    "sensor": {
      "last_sync": { "name": "Last sync" },
      "files_backed_up": { "name": "Files backed up" },
      "last_error": { "name": "Last error" },
      "free_space": { "name": "Free space" }
    }
  },
  "services": {
    "backup_now": {
      "name": "Run backup now",
      "description": "Immediately starts a backup run, regardless of the configured interval.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry",
          "description": "The Google Photos Backup instance to sync."
        }
      }
    },
    "start_picker_session": {
      "name": "Start photo picker session",
      "description": "library_api backend only: creates a Picker session and notifies you with the selection link.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry",
          "description": "The Google Photos Backup instance with backend library_api."
        }
      }
    }
  }
}
